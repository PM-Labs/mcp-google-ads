# .fork-sync-ignore — upstream commits fork-sync must skip.
#
# One SHA per line, optional reason after whitespace.
# Blank lines and # comments allowed.
#
# See CLAUDE.md in pmin-mcpinfrastructure for the full convention. When an
# upstream commit is listed here, the weekly sync cron on mcp-server will
# silently skip it instead of tripping a merge conflict.
#
# Listed SHAs must exist in `upstream/main` — otherwise the sync script
# will log a warning and treat the entry as a no-op.

# --- mcp-google-ads: reject upstream OAuth reimplementation (2026-04-13) ---
# Upstream implemented their own OAuth proxy in googleads/mcp-google-ads#46
# (commit 3e0d25c + its PR merge 320dd4c). We already have our own OAuth proxy
# plus the no-session-resurrection fix (PM-Labs/mcp-google-ads b2b6a33..246ac3d),
# which is incompatible with upstream's approach. Keep ours.
#
# The standalone non-merge commits contributing to the OAuth change and related
# packaging/lint noise that we also don't want:

3e0d25c  upstream OAuth proxy implementation — we have our own + session-resurrection fix
958f8b8  MANIFEST.in .txt addition — we build via docker, not pypi
45f609d  README-only update for upstream OAuth docs — not relevant to our fork
de051e6  upstream lint pass — noise, not worth the conflict surface

# --- mcp-google-ads: skip Windows deadlock fix (2026-06-07) ---
# f953ff4 adds a context manager to prevent stdio handle inheritance on Windows.
# We run Linux containers — this is a no-op on Linux and conflicts with our fork.
f953ff4  Windows-only stdio deadlock fix — not applicable to Linux containers

# --- mcp-google-ads: configurable-tools upstream PR (2026-06-11) ---
# Upstream PR #82 added tools_config.yaml configurable tools across 5 commits.
# All 5 are already cherry-picked into our fork under different SHAs
# (19fb66c, 6a6353d, ff8ecb4, db7ba7e, 88eeebc) with minor patch divergence.
5df2a4e  already applied as 19fb66c (Make the exposed tools configurable with the tools_config.yaml file)
cb681d7  already applied as 6a6353d (Ensure the schema generated for the search tool)
45f42be  already applied as ff8ecb4 (Make the exposed tools configurable — second commit)
fdda341  already applied as db7ba7e (Rename the search tool in tests)
cb80934  already applied as 88eeebc (fix: bundle default tools_config.yaml)

# --- mcp-google-ads: PbMessage serialization fix (2026-07-26) ---
# Upstream 8594723 adds serialization of bare PbMessage instances.
# Already applied to our fork as 1a8f2e9 (cherry-pick with minor patch divergence).
8594723fddbdac0c58badd9b557f7e507e29eb4b  already applied as 1a8f2e9 (serialize bare PbMessage instances in format_output_value)

# --- mcp-google-ads: upstream commit 2d01b97e (2026-07-26) ---
# This commit conflicts with our OAuth proxy implementation (our fork has a custom
# OAuth flow via PM-Labs/mcp-google-ads b2b6a33..246ac3d; upstream's 2d01b97 adds
# credential logging changes that diverge from our approach).
# TODO: manually cherry-pick the non-OAuth parts if needed in a future session.
2d01b97e  skip — conflicts with our OAuth proxy implementation

# --- mcp-google-ads: FastMCP production auth configuration (2026-08-11) ---
# Upstream PR #95 (f8130b1) adds configurable JWT token storage backends
# (filetree/redis/memory) and wires jwt_signing_key into FastMCP GoogleProvider.
# Our fork removed GoogleProvider entirely — we auth at the Cloudflare/Caddy
# proxy level. f8130b1 conflicts with our coordinator.py and provides no benefit.
f8130b1  skip — conflicts with coordinator.py; we auth at proxy level not via FastMCP GoogleProvider
150bbce  merge commit for PR #95 (production auth configuration) — listed for audit hygiene

# --- mcp-google-ads: remove account-performance-diagnostics skill (2026-08-11) ---
# Upstream PR #97 (38ac5b1) removes the skill (moved to upstream skills repo).
# Applied cleanly to our fork as 4bd4696.
38ac5b1  applied as 4bd4696 (remove account-performance-diagnostics SKILL.md)
91beff7  merge commit for PR #97 — listed for audit hygiene

# --- mcp-google-ads: Firestore storage backend (2026-08-27) ---
# 8e164ff was manually cherry-picked on 2026-08-26 as fork commit 6f4d1b5.
# Patch equivalence did not match the resolved cherry-pick; listed to suppress re-detection.
8e164ff59e8c49b29b2fd8e6149f9b80cdc9f5aa  already applied as 6f4d1b5
f0735c1  merge commit for upstream PR 104 - audit hygiene

# a11b95b applied manually as 6bd18f1 with conflict resolution (v25 token snapshot diffs)
a11b95b8ad9e8dba0482746a4e7d0ba086f27220

# 32c804f applied manually as 7ef080e with conflict resolution (FastMCP 4.0.3 upgrade)
32c804f15126db32e85d47fa3215626fb79d7893
